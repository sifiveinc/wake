# Test: warning for unregistered paths

from wake import _

export def makeFiles (args: List String) =
    def content = "test content"

    map (write _ content) args
    | findFail
    | rmap (\_ Unit)
