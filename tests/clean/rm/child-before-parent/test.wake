# Test: child directory specified before parent

from wake import _

export def makeFiles (args: List String) =
    def content = "shared"

    map (write _ content) args
    | findFail
    | rmap (\_ Unit)
